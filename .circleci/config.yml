version: 2.1

jobs:
  build-code:
    docker:
      - image: cimg/go:1.19.4
    steps:
      - checkout  
      - run:
        name: Build Code Command
        command: |
          cd ~/project/
          go build main.go

workflows:
  deploy-code:
    jobs:
      - build-code
